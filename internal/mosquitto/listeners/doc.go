// Package listeners models, renders, parses, and validates Mosquitto listener configuration blocks.
//
// It intentionally has no storage, HTTP, restart runner, or frontend integration; those concerns belong to later work units.
package listeners

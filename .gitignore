# Build outputs
bin/
# Locally built binaries dropped at the repo root (e.g. `go build ./cmd/...`)
/mcm
frontend/dist/*
!frontend/dist/index.html
frontend/node_modules/

# Go test artifacts
*.test
coverage.out
